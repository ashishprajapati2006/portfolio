// Placeholder types file
export interface User {
  id: string;
  email?: string;
  username?: string;
}

export interface Profile {
  id: string;
  username?: string;
  email?: string;
  avatar_url?: string;
}
